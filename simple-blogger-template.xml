<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="fr">
<head>
    <title><data:blog.title/></title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&amp;family=Inter:wght@300;400;500;600&amp;display=swap" rel="stylesheet" />
    <script type="text/javascript" async
        src="https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.7/MathJax.js?config=TeX-MML-AM_CHTML">
    </script>
    <b:skin><![CDATA[
        /* Palette de couleurs */
        :root {
            --sky-blue: #87CEEB;
            --sky-blue-light: #B3E0F7;
            --sky-blue-dark: #5D9EC9;
            --pure-white: #FFFFFF;
            --off-white: #F8FAFC;
            --light-gray: #E8F4FC;
            --text-dark: #2C3E50;
            --text-medium: #546E7A;
            --accent-blue: #3498DB;
            --shadow-light: rgba(135, 206, 235, 0.1);
            --shadow-medium: rgba(135, 206, 235, 0.15);
            --gradient-sky: linear-gradient(135deg, var(--sky-blue) 0%, var(--sky-blue-light) 100%);
        }

        /* Reset et base */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            scroll-padding-top: 80px;
        }

        body {
            font-family: 'Inter', 'Poppins', Arial, sans-serif;
            color: var(--text-medium);
            background-color: var(--off-white);
            line-height: 1.7;
            overflow-x: hidden;
            font-weight: 400;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        /* Typographie */
        h1, h2, h3, h4 {
            font-family: 'Poppins', sans-serif;
            color: var(--text-dark);
            font-weight: 600;
            line-height: 1.3;
            margin-bottom: 1rem;
        }

        h1 {
            font-size: 2.5rem;
            color: var(--sky-blue-dark);
            background: var(--gradient-sky);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 1.5rem;
        }

        h2 {
            font-size: 1.8rem;
            padding-bottom: 0.5rem;
            border-bottom: 3px solid var(--sky-blue-light);
            position: relative;
            margin-top: 2rem;
        }

        h2::after {
            content: '';
            position: absolute;
            bottom: -3px;
            left: 0;
            width: 60px;
            height: 3px;
            background: var(--gradient-sky);
            border-radius: 2px;
        }

        h3 {
            font-size: 1.4rem;
            color: var(--sky-blue-dark);
            margin-top: 1.5rem;
        }

        p {
            margin-bottom: 1.2rem;
            font-size: 1.05rem;
        }

        /* Hero Section Moderne */
        .hero-section {
            position: relative;
            width: 100%;
            height: 400px;
            background: 
                linear-gradient(rgba(135, 206, 235, 0.85), rgba(255, 255, 255, 0.9)),
                url('https://images.unsplash.com/photo-1558494949-ef010cbdcc31?ixlib=rb-4.0.3&auto=format&fit=crop&w=2074&q=80') no-repeat center center/cover;
            display: flex;
            justify-content: center;
            align-items: center;
            border-radius: 0 0 20px 20px;
            overflow: hidden;
            box-shadow: 0 10px 30px var(--shadow-medium);
            margin-bottom: 2.5rem;
        }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--gradient-sky);
            opacity: 0.1;
        }

        .home-button {
            position: absolute;
            top: 25px;
            left: 25px;
            background: var(--pure-white);
            color: var(--sky-blue-dark);
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: 600;
            padding: 10px 20px;
            border: 2px solid var(--sky-blue-light);
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            z-index: 10;
            box-shadow: 0 4px 15px var(--shadow-light);
        }

        .home-button:hover {
            background: var(--gradient-sky);
            color: var(--pure-white);
            transform: translateY(-2px);
            box-shadow: 0 6px 20px var(--shadow-medium);
            border-color: transparent;
        }

        .hero-text {
            font-size: 3.2rem;
            font-weight: 700;
            color: var(--text-dark);
            text-align: center;
            padding: 30px;
            position: relative;
            z-index: 2;
            text-shadow: 2px 2px 4px rgba(255, 255, 255, 0.8);
        }

        /* Navigation Sociale */
        .social-nav {
            background: var(--pure-white);
            padding: 15px 20px;
            border-radius: 12px;
            box-shadow: 0 5px 20px var(--shadow-light);
            margin-bottom: 2rem;
            border: 1px solid var(--light-gray);
        }

        .social-links {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 25px;
            flex-wrap: wrap;
        }

        .social-links a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 45px;
            height: 45px;
            background: var(--pure-white);
            border: 2px solid var(--sky-blue-light);
            border-radius: 50%;
            color: var(--sky-blue-dark);
            font-size: 1.2rem;
            text-decoration: none;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .social-links a::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: var(--gradient-sky);
            transition: left 0.3s ease;
            z-index: -1;
        }

        .social-links a:hover {
            transform: translateY(-5px) scale(1.1);
            color: var(--pure-white);
            border-color: transparent;
            box-shadow: 0 8px 20px var(--shadow-medium);
        }

        .social-links a:hover::before {
            left: 0;
        }

        /* Layout Principal */
        .main-container {
            display: flex;
            gap: 40px;
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Contenu Principal */
        .main-content {
            flex: 3;
            min-width: 0;
        }

        .blog-post {
            background: var(--pure-white);
            padding: 2rem;
            border-radius: 15px;
            margin-bottom: 2rem;
            box-shadow: 0 5px 20px var(--shadow-light);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: 1px solid var(--light-gray);
        }

        .blog-post:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px var(--shadow-medium);
        }

        .post-date {
            color: var(--sky-blue-dark);
            font-size: 0.9rem;
            font-weight: 500;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .post-date::before {
            content: '📅';
            font-size: 0.8rem;
        }

        /* Sidebar */
        .sidebar {
            flex: 1;
            min-width: 280px;
        }

        .sidebar-section {
            background: var(--pure-white);
            padding: 1.8rem;
            border-radius: 15px;
            margin-bottom: 2rem;
            box-shadow: 0 5px 20px var(--shadow-light);
            border: 1px solid var(--light-gray);
        }

        .sidebar-section h2 {
            font-size: 1.4rem;
            border: none;
            padding-bottom: 0;
            margin-bottom: 1.2rem;
        }

        .sidebar-section h2::after {
            width: 40px;
        }

        /* Widget Profil */
        .profile-container {
            text-align: center;
        }

        .profile-img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            margin: 0 auto 1.2rem;
            border: 4px solid var(--sky-blue-light);
            padding: 4px;
            background: var(--pure-white);
            box-shadow: 0 5px 15px var(--shadow-light);
        }

        .profile-name {
            color: var(--sky-blue-dark);
            font-size: 1.4rem;
            margin-bottom: 0.8rem;
        }

        .profile-bio {
            color: var(--text-medium);
            font-size: 0.95rem;
            line-height: 1.6;
        }

        /* Éléments de contenu */
        .highlight-box {
            background: linear-gradient(135deg, var(--off-white) 0%, var(--light-gray) 100%);
            padding: 1.5rem;
            border-left: 5px solid var(--sky-blue);
            border-radius: 0 8px 8px 0;
            margin: 1.5rem 0;
            position: relative;
            overflow: hidden;
        }

        .highlight-box::before {
            content: '💡';
            position: absolute;
            top: 10px;
            right: 15px;
            font-size: 1.5rem;
            opacity: 0.3;
        }

        blockquote {
            margin: 1.5rem 0;
            padding: 1.5rem 2rem;
            background: linear-gradient(135deg, #E8F4FC 0%, #F0F9FF 100%);
            border-left: 5px solid var(--sky-blue);
            font-style: italic;
            color: var(--text-dark);
            border-radius: 0 10px 10px 0;
            position: relative;
            overflow: hidden;
        }

        blockquote::before {
            content: '"';
            position: absolute;
            top: -20px;
            left: 10px;
            font-size: 4rem;
            color: var(--sky-blue-light);
            opacity: 0.3;
            font-family: Georgia, serif;
        }

        pre {
            background: var(--text-dark);
            color: var(--light-gray);
            padding: 1.5rem;
            border-radius: 8px;
            overflow-x: auto;
            margin: 1.5rem 0;
            font-family: 'Courier New', monospace;
            font-size: 0.95rem;
            line-height: 1.5;
            border-left: 4px solid var(--sky-blue);
        }

        code {
            background: var(--light-gray);
            color: var(--sky-blue-dark);
            padding: 2px 6px;
            border-radius: 4px;
            font-family: 'Courier New', monospace;
            font-size: 0.9rem;
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
            margin: 1.5rem 0;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 15px var(--shadow-light);
        }

        th {
            background: var(--gradient-sky);
            color: var(--pure-white);
            font-weight: 600;
            padding: 1rem;
            text-align: left;
            border: none;
        }

        td {
            background: var(--pure-white);
            padding: 1rem;
            border-bottom: 1px solid var(--light-gray);
        }

        tr:hover td {
            background: var(--off-white);
        }

        /* Liens */
        a {
            color: var(--sky-blue-dark);
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            border-bottom: 1px dotted transparent;
        }

        a:hover {
            color: var(--sky-blue);
            border-bottom: 1px dotted var(--sky-blue);
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 2.5rem 20px;
            background: var(--pure-white);
            border-top: 1px solid var(--light-gray);
            margin-top: 3rem;
            color: var(--text-medium);
        }

        .footer-content {
            max-width: 800px;
            margin: 0 auto;
        }

        footer p {
            margin-bottom: 0.5rem;
            font-size: 0.95rem;
        }

        .footer-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 1rem;
            flex-wrap: wrap;
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .blog-post, .sidebar-section {
            animation: fadeIn 0.6s ease-out;
        }

        /* Responsive Design */
        @media (max-width: 1024px) {
            .main-container {
                gap: 30px;
            }
            
            .hero-text {
                font-size: 2.8rem;
            }
        }

        @media (max-width: 768px) {
            .main-container {
                flex-direction: column;
                padding: 0 15px;
            }
            
            .hero-section {
                height: 300px;
                border-radius: 0 0 15px 15px;
            }
            
            .hero-text {
                font-size: 2.2rem;
                padding: 20px;
            }
            
            .home-button {
                top: 20px;
                left: 20px;
                padding: 8px 16px;
                font-size: 0.9rem;
            }
            
            .social-links {
                gap: 15px;
            }
            
            .social-links a {
                width: 40px;
                height: 40px;
                font-size: 1.1rem;
            }
            
            h1 {
                font-size: 2rem;
            }
            
            h2 {
                font-size: 1.6rem;
            }
            
            .blog-post, .sidebar-section {
                padding: 1.5rem;
            }
        }

        @media (max-width: 480px) {
            .hero-text {
                font-size: 1.8rem;
            }
            
            h1 {
                font-size: 1.8rem;
            }
            
            h2 {
                font-size: 1.4rem;
            }
            
            .social-links {
                gap: 12px;
            }
            
            .social-links a {
                width: 38px;
                height: 38px;
            }
            
            .footer-links {
                flex-direction: column;
                gap: 10px;
            }
        }

        /* Scrollbar personnalisée */
        ::-webkit-scrollbar {
            width: 10px;
        }

        ::-webkit-scrollbar-track {
            background: var(--off-white);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--gradient-sky);
            border-radius: 5px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: var(--sky-blue-dark);
        }
    ]]></b:skin>
</head>
<body>
    <!-- Hero Section -->
    <div class="hero-section">
        <a href="https://solomon-tessema.github.io" class="home-button" title="Accueil">
            <i class="fas fa-home"></i> Accueil
        </a>
        <div class="hero-text">
            <data:blog.title/>
        </div>
    </div>

    <!-- Layout Principal -->
    <div class="main-container">
        <!-- Contenu Principal -->
        <main class="main-content">
            <section id="blog-posts">
                <b:section id="main" class="blog-posts" showaddelement="yes">
                    <b:widget id="Blog1" type="Blog" title="Articles Récents" />
                </b:section>
            </section>
        </main>

        <!-- Sidebar -->
        <aside class="sidebar">
            <!-- Navigation Sociale -->
            <nav class="social-nav">
                <div class="social-links">
                    <a href="https://solomon-tessema.github.io" target="_blank" title="Site Web">
                        <i class="fas fa-globe"></i>
                    </a>
                    <a href="https://www.linkedin.com/in/solomon-tessema" target="_blank" title="LinkedIn">
                        <i class="fab fa-linkedin-in"></i>
                    </a>
                    <a href="https://github.com/solomon-tessema" target="_blank" title="GitHub">
                        <i class="fab fa-github"></i>
                    </a>
                    <a href="https://www.kaggle.com/solomonTTessema" target="_blank" title="Kaggle">
                        <i class="fab fa-kaggle"></i>
                    </a>
                    <a href="https://public.tableau.com/profile/solomontessema/vizzes" target="_blank" title="Tableau">
                        <i class="fas fa-chart-bar"></i>
                    </a>
                    <a href="https://datascienceelements.blogspot.com" target="_blank" title="Blog">
                        <i class="fas fa-blog"></i>
                    </a>
                </div>
            </nav>

            <!-- Section Profil -->
            <div class="sidebar-section">
                <b:section id="profileSection" class="profile-section" showaddelement="yes">
                    <b:widget id="Profile1" type="Profile" title="À Propos" />
                </b:section>
            </div>

            <!-- Archives -->
            <div class="sidebar-section">
                <b:section id="archiveSection" class="archive-section" showaddelement="yes">
                    <b:widget id="BlogArchive1" type="BlogArchive" title="Archives" />
                </b:section>
            </div>
        </aside>
    </div>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <p>&copy; <data:blog.title/> | Conçu par Solomon Tessema</p>
            <div class="footer-links">
                <a href="#top">↑ Haut de page</a>
                <a href="/p/contact.html">Contact</a>
                <a href="/p/privacy.html">Confidentialité</a>
            </div>
        </div>
    </footer>

    <!-- Script pour animations supplémentaires -->
    <script>
        // Animation au défilement
        document.addEventListener('DOMContentLoaded', function() {
            const observerOptions = {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            };
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = "1";
                        entry.target.style.transform = "translateY(0)";
                    }
                });
            }, observerOptions);
            
            // Observer les articles et sections
            document.querySelectorAll('.blog-post, .sidebar-section').forEach(el => {
                el.style.opacity = "0";
                el.style.transform = "translateY(20px)";
                el.style.transition = "opacity 0.6s ease, transform 0.6s ease";
                observer.observe(el);
            });
            
            // Smooth scroll pour les ancres
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    e.preventDefault();
                    const targetId = this.getAttribute('href');
                    if(targetId === '#') return;
                    
                    const targetElement = document.querySelector(targetId);
                    if(targetElement) {
                        window.scrollTo({
                            top: targetElement.offsetTop - 80,
                            behavior: 'smooth'
                        });
                    }
                });
            });
        });
    </script>
</body>
</html>
